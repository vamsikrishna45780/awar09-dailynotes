

EC2 : Elastic Compute Cloud : We can launch servers, We can deploy applications into the servers, and we can deliver to the world / customers (B2B - Business to Business).. 

95 % Applications -  B2B model
5% Open to the world - google, Meta, openai, anthropic, nexflix, prime... 


laptop : cpu, motherboard, memory, storage, GPU, Graphic Card.. : CLient class hardware.. Client class OS : 
windows 10, windows 7, ubuntu desktop, macos..  i3, i5, i7 processors, amd processor..

Server : cpu, motherboard, memory, storage, GPU, Graphic Card..  : So powerfull, we can run 24x7 and 365 days..
RHEL, Ubuntu server, Amazon Linux, Windows server 2022, 2026.. intel xeon, graviton, amd... 
 
EC2 is to run Servers.. Not client OS.. 

Pay-as-you-go Model : 

Server for 1 hr.. run it for 1 hr.. Upgrade it.. downgrade.. Delete : Pay for your actual usage.. 
DB / Network / Monitoring / Encry / Storage.. 

---

Server = Instance = Azure VM = Virtual Machiene = GCP Compute 

---

1. On-Demand Instances : Default pricing model in AWS.

Pay only what you use.
No long-term commitment
Changed per Second basis (Min 60 Sec)
Highest flexibility and Highest Cost

Best for : Testing, short duration runs, Temp environment (dev/qa/uat)


2. Reserved Instances (RI)

Commitment required for 1 Yr / 3 yrs
We get upto ~70% discount compared to On-Demand.

	2.1 Standard RI : Maximum Discount, Less Flexibility to change configuration (Fixed Instance family/type)
	2.2 Convertable RI : Lower Discount, More flexibility to change Instance Family / OS / Tenancy.

Payment Options for RI : 
	All Upfront : Pay everything at the time of purchase, No monthly cost.
	Partial Upfront : YOu pay 30-60% now, AWS Decrease the hourly cost of the instance.. Monthly cost occurs, 	but with redused cost..
	No Upfront : Pay everything monthly basic.. Cost similar to On-Demand..


3. Saving Plans : THis is modern replacement/improvement over RIs. AWS intrododused this to reduse the RIs complexity. you pay it whether you use it or not

We are commiting with AWS on Hourly price. You are giving commitment to aws to spend 10$/Hr, so AWS offers good amount of discount.

	3.1 Compute Saving Plan : For multiple services : ec2, lambda, fargate.. We can change Instance fanily / size / Region / OS / AZs.. 
	
	3.2 EC2 instance savings plan : Less flexible but good discount.. Applicable to only EC2 service.. We can change Instance family in a specific region..

3.3 SageMaker Saving Plan : AI/ML related workload 
3.4 Database Saving Plan : Database realted worklaod


4. Spot Instances : AWS Offers its unused capacity at huge discount. BUT ..... AWS Can terminate/Delete the instance anytime with Just 2 min warning. 
Batch Job, Rendering, Temp Jobs, Easily recoverable jobs, Interruptable jobs..

--> If Price Not increase, YOU Terminated our INstance, We need to pay full amount..
--> If price increased, AWS terminated our instance, we get charged for FUll hours only, We dont get charged for Partial hours..

5 hrs 50 Minutes.. 

YOU Terminate : 5 hrs 50 min
AWS Terminate : 5 hrs only

Not good for Production workload / DBs / Stateful systems.. 

--> We have to bid against aws pricing. if our bid price is more than or equal, AWS provides the server.

===================


Step 1 : Add Tags (Name, OS, Cost Center, Project, Client)

Step 2 : Choose AMI (Amazon Machine Image) - Select Required OS (Amazon Linux)

Choose required OS.. 

Step 3 : Choose Instance Type (CPU, Memory) (t3.micro)

t --> Testing/Tiny
M --> Mixed / balanced
C --> CPU heavy
R --> RAM heavy
P/G --> GPU
I/D/H --> Intensive Storage

t2 --> Older
t3 --> newer
t4g --> ARM/Graviton based (Cost efficient)

small website --> t3.micro
application server --> m5.large
CPU intensive app --> c6i.large
AI Model training --> p5 / trn1

Remember this config:
C5.xlarge - 4 cpu, 8 gb memory
m5.4xlarge - 16 cpu, 64 gb memory


Step 4 : Choose keyPair (helps to authenticate the server) (No keypair)

How to connect to it..? SSH.. Putty / Mobaxterm / terminal / Command prompt / github terminal / superputty

Username : amazon linux - ec2-user / ubuntu - ubuntu / redhat - redhat

Linux : Works with Public Key and PRivate key authentication method. 
AWS Copy Public key inside ec2 instance.. AWS Provides Private key to us.. Now, to connect to instance, you need to browse your private key, against public key, If matches, then allow us to connect.. 


Step 5 : Choose Network Settings (VPC - Virtual Private Cloud) (SSH --> Anywhere)

Firewall (security groups): A security group is a set of firewall rules that control the traffic for your instance. Add rules to allow specific traffic to reach your instance.

SSH : 22 : --> My IP

Step 6 : Choose the required storage size. (Linux : 8 Gb.. Windows: 30 gb)

========

1. Terminal / Comamnd Prompt : 

windows users : open comamnd prompt / Powershell : type "ssh" 
ssh is not a recognised command / not an operable program.. Enable "openssh" in your programs. 

Go to System > Optional features 
Look through the list for "OpenSSH Client." Select View features (Windows 11) or Add a feature (Windows 10).Search for "OpenSSH Client," select it, and click Install. CLose and re-open the terminal.. type "ssh"

Navigate to folder, wehere you have the keypair.. In Address bar, type "cmd" and click on enter.. 

ssh -i <keypair> <username>@<instance-public-ip> 

ssh -i "awar09-kp.pem" ec2-user@13.206.208.205

---

unprotected file error in windows os:  WARNING: UNPROTECTED PRIVATE KEY FILE! (or) Permisisons are too open.

icacls <keypair.pem> /inheritance:r
icacls <keypair.pem> /grant:r "$($env:USERNAME):(R)"

icacls <keypair.pem> /remove "Users"
icacls <keypair.pem> /remove "Everyone"
icacls <keypair.pem> /remove "Authenticated Users"

---

https://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html





















